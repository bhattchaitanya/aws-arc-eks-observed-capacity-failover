apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: arc-eks-24h-east
  region: us-east-2
  tags:
    Project: arc-eks-24h-lab
    Environment: experiment
    ManagedBy: eksctl

vpc:
  cidr: 10.82.0.0/16
  clusterEndpoints:
    publicAccess: true
    privateAccess: true
  nat:
    gateway: Disable

iam:
  withOIDC: true

autoModeConfig:
  enabled: true
  nodePools:
    - general-purpose
